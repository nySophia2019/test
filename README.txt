# The dashboard will show a dynamic dashboard of sales in three layers, namely 'Country', 'State' and 'Dealsize' filters respectively. 
# You can change the select conditions in individual filters and the dashboard will demonstrate results accordingly. 
# the dataset is a public dataset downloaded from https://www.kaggle.com/datasets/kyanyoga/sample-sales-data

# run the app.py to trigger the dashboard coding. 

# enter the commond "streamlit run ./Common/app.py" in Terminal commond line to browse the dashboard on your browser. 

# You can change your filter conditions as you wish. 